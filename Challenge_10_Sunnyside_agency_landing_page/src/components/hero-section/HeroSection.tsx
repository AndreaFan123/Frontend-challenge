import { HeaderContainer } from "./HeroSection.styled";

const HeroSection = () => {
  return (
    <HeaderContainer>
      <h1>we are creatives</h1>
    </HeaderContainer>
  );
};

export default HeroSection;
